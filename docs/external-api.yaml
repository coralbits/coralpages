title: "API get example"
template: "page://_default"

data:
  - type: markdown
    data:
      text: |
        # next is data directly from an external API

  - type: text-api
    data:
      text: |
        This data is sent to another server, which renders the component and then is included into the
        generated page.
